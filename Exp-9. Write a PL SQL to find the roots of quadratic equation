Exp-9. Write a PL/ SQL to find the roots of quadratic equation

9:
declare
    a number :=5;
    b number :=6;
    c number :=2;
    d number ;
    r1 number ;
    r2 number ;
begin
    d:=b*b-4*a*c;
    if(d>0) then
        r1:=(-b+sqrt(d))/(2*a);
        r2:=(-b-sqrt(d))/(2*a);
        dbms_output.put_line('the roots are' ||r1|| 'and' ||r2);
else if(d=0) then
    r1:=-b/(2*a);
    dbms_output.put_line('the roots are' ||r1|| 'and' ||r1);
else
    r1:=-b/(2*a);
    r2:=sqrt(-d)/(2*a);
    dbms_output.put_line('the roots are' ||r1|| 'and' ||'+i'||r2||r1||'-i'||r2);
    end if;
end if;
end;

