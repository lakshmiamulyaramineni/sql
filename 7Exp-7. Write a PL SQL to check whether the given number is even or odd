7:Exp-7. Write a PL/ SQL to check whether the given number is even or odd
declare
    a number :=2;
begin
    if(mod(a,2)=0) then
        dbms_output.put_line(a || 'is even');
    else
        dbms_output.put_line(a || 'is odd');
    end if;
end;
